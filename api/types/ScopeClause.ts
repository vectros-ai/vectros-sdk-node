// This file was auto-generated by Fern from our API Definition.

/**
 * Inline scope clauses granted to this trigger's principal — the same shape a role's own clause list carries. Mutually exclusive with 'roleIds'; provide exactly one.
 */
export interface ScopeClause {
    /**
     * Verbs this clause grants, in the compact 'resource:ops[:qualifier]' form the authorizer honors. 'ops' are the letters c/r/u/d (create/read/update/delete) plus 's' for sensitive-field REVEAL (PHI un-masking), e.g. 'records:cru', 'documents:r', 'records:rs:patient', plus 'x' to EXECUTE a stored script synchronously ('scripts:x', or 'scripts:x:<name>' for one script name, every version). The 's' letter is fail-safe: absent it, sensitive fields are masked at the response boundary. The standard catalog is '*' (wildcard), 'keys:crd' (manage scoped API keys), 'profiles:cru' (manage access profiles), 'app-contexts:cru', 'users:crud' (manage your tenant's own users, including sub-user invitations), and 'logs:r'; you may add custom 'resource:ops' verbs for application-specific resources. Bare verbs like 'read'/'write'/'delete' are NOT grantable — use the resource:ops form. A ':qualifier' segment is honored only where it actually narrows the grant — 'records'/'entities' correlate it for every op; 'documents'/'users' correlate it ONLY for the 's' op (e.g. 'documents:s:invoice' restricts sensitive-field reveal to that type); 'profiles' correlates it ONLY for c/u/d (create/update/delete), confining WHICH principal the grant may act on — a literal 'usr_<id>' principal, or the bare 'self' sentinel, which matches only when the target principal is your own bound identity (a bare, unqualified 'profiles:cru' stays broad and unaffected); 'scripts' correlates it ONLY for the 'x' op, naming WHICH script may be executed ('scripts:x:create-case-folder' — every version of that name). A qualifier on any other resource/op combination — e.g. 'documents:r:invoice' or 'profiles:r:self' — is rejected at authoring time rather than silently ignored. The 'x' letter itself is accepted on every resource but has an effect only on 'scripts'; 'records:x' is valid and grants nothing.
     *
     * One cross-field caveat: if this clause also carries a 'granted_capabilities' entry that this release does not recognize, the WHOLE clause is denied and none of these verbs apply. See that field.
     */
    allowed_actions: string[];
    /**
     * Attribute filters narrowing the data this clause applies to, keyed per ownership dimension: 'userId' (the owning user) and namespaced scopes 'scope:<namespace>' ('scope:org', 'scope:client', 'scope:group', ...). Use '*' as the key to state a rule for every dimension this clause does not name explicitly; a named dimension always takes precedence over it. Multiple keys must ALL match (AND).
     *
     * Allowed values are ids, plus: null — records with NO value in that dimension (e.g. {"scope:org": ["orgX", null]} means orgX's records AND unowned ones); '${{ any }}' — any value in that dimension, but NOT records lacking one, so combine it with null to cover both; '${{ self.userId }}' / '${{ self.scope.<namespace> }}' — the credential's own value, resolved per request; '${{ under.self.userId }}' / '${{ under.self.scope.<namespace> }}' — values whose immediate parent is the credential's own (one level, not a full ancestor walk); '${{ member.scope.<namespace> }}' / '${{ member.scope.<namespace>:<level> }}' — every value of that namespace the credential's principal is a MEMBER of, per the namespace's own membership declaration (see 'POST /v1/namespaces'), optionally narrowed to one membership level. This opts in explicitly — a namespace that declares no membership backing grants nothing to either form — and resolves once per request, so a revoked membership takes effect on your very next request rather than waiting out the token's lifetime. Naming a level the namespace has not declared is rejected at authoring time. Any other '${{ ... }}' spelling is rejected.
     *
     * Reads and writes differ where a dimension is OMITTED: reading is not narrowed by a dimension the clause says nothing about, but writing a value into that dimension is not authorized by silence — a clause must name a dimension (or '*') to place data there. An empty object therefore reads tenant-wide and authorizes no placement.
     */
    data_scope?: Record<string, Record<string, unknown>> | undefined;
    /**
     * Named platform capabilities this clause grants — bounded effects that reach across a partition boundary, which allowed_actions cannot express.
     *
     * Six names are accepted in this release.
     *
     * 'member-lifecycle' — create and remove identities in your tenant. Adding someone who already has an identity here resolves them by email or externalId as part of that operation; it does not let the credential look an identity up on its own. Grant it deliberately: its reach is any app context the credential can already administer, which is an ordinary grant where contexts separate your APPS — but not where you have modelled your own customers as separate app contexts.
     *
     * 'forensic-read' — read the access log across every context in your tenant, by the credential that performed each read ('what did key K read'). It confers nothing on its own: the endpoint still requires the 'access-log:r' action, and this capability only lifts the app-context confinement on the by-credential query — so the effective grant is 'access-log:r' PLUS this name. It is tenant-wide by design and is not narrowed by the credential's own app context, so grant it only to an audit or support role. It is refused when a self-signup policy creates a profile, so no NEW self-signup can be created against a role that holds it; note that this is checked at that moment rather than continuously, so adding it to a role your users can already sign up to grants it to those existing members at their next token.
     *
     * 'context-directory-read' — read your tenant's app-context DIRECTORY (which app contexts exist, their roles and access profiles, and which contexts a given principal is in) across every context, not just the credential's own. It confers no data-plane reach — records, documents, folders, schemas and entities are outside it entirely — and no write. Its effect: GET /v1/principals/{id}/profiles for a principal OTHER than the credential's own returns that principal's access profile in the credential's own app context only, unless the credential holds this capability, in which case it returns the profile across every context you administer (the same answer a lookup of the credential's OWN principal always gets). Grant it only to an admin or support role, for the same reason as 'forensic-read': it is tenant-wide by design and not narrowed by the credential's own app context. It is refused when a self-signup policy creates a profile, so no NEW self-signup can be created against a role that holds it; that check runs at that moment rather than continuously, so adding it to a role your users can already sign up to grants it to those existing members at their next token.
     *
     * 'delegate-mint' — mint a scoped API key (ssk_*) bound to a DIFFERENT principal than your own, when creating one via POST /v1/admin/keys/scoped. Without it, that endpoint only lets a scoped credential mint a key bound to itself; a root key is unaffected. The target must already be a member of your own app context, and the minted key's effective scope can never exceed your own — this capability lifts only the WHO bound, not the WHAT one. Grant it to an operator or automation role that provisions credentials on other members' behalf; it is tenant-wide by design (the credential it lets you mint IS someone else, wherever that identity is later used). It is refused when a self-signup policy creates a profile, so no NEW self-signup can be created against a role that holds it; that check runs at that moment rather than continuously, so adding it to a role your users can already sign up to grants it to those existing members at their next token.
     *
     * 'delegate-principal-stamp' — when creating a record, stamp a userId belonging to someone else in your tenant instead of only your own. Without it, a userId on create must match the credential's own identity or the request is rejected. This capability does not by itself admit any value: the SAME data_scope clause that already authorizes the write must ALSO explicitly name userId (a clause silent on userId still denies), so you keep exact, per-value control over which principals may be named. The capability itself does NOT need to live on that same clause — a role composed from separate roleIds, one contributing the capability and another the admitting data_scope, is checked the same as a role that declares both on one clause; what must be co-located is the write's own verb and its userId admission, not the capability. It confers no ability to act as that person — the credential never gains their authority, only the ability to label a record as theirs, which becomes usable only once they authenticate with their own credential. Unlike the three capabilities above, its reach is NOT tenant-wide: it is bounded by the SAME record type and app context your data_scope clause already confines the write to. It applies to record creation only — an update can never reassign userId, with or without this capability. Grant it to an admin role that provisions membership-shaped records on behalf of known users. Unlike the three capabilities above, it is NOT refused when a self-signup policy creates a profile — a context founder can hold it immediately so they can onboard teammates from the moment their context exists.
     *
     * 'trigger-control-plane-grant' — let a trigger rule's own grant (its 'scopes', or a composed role) carry a control-plane verb: any operation on 'keys', 'profiles', 'users', 'app-contexts', or 'access-log', or any operation on 'scripts' — or the bare wildcard '*', which grants every resource. Without it, POST/PUT /v1/triggers rejects a grant naming one of those resources or '*', even when you already hold the verb yourself — a trigger's grant executes UNATTENDED, on every matching record write, for as long as the rule exists, which is a materially different exposure than a human exercising the same scope once. This capability does not by itself admit any value: the same clause (or composed role) still has to be something you are separately authorized to declare — ordinary grant-subset checking still applies, so you cannot use this capability to grant a trigger authority you do not hold. It is tenant-wide by design, for the same reason as 'forensic-read'/'delegate-mint' — grant it deliberately, to an operator or automation role that provisions durable, control-plane-capable automation on purpose, not by default. It is refused when a self-signup policy creates a profile, so no NEW self-signup can be created against a role that holds it; that check runs at that moment rather than continuously, so adding it to a role your users can already sign up to grants it to those existing members at their next token.
     *
     * The list is CLOSED and versioned with the API: a later release may accept further names, and a name this release does not accept is rejected when you author the clause. Three rules, all fail-closed: an ABSENT or empty list grants NO capability (absence is silence, not a wildcard); an UNRECOGNIZED name denies this whole clause rather than being ignored, so a typo fails loudly at the first request instead of silently downgrading the grant; and a '*' in allowed_actions confers ZERO capabilities — '*' is not a wildcard here and is rejected as a name. Each capability's reach is fixed by its own definition, stated above; the clause carries NAMES ONLY. Where a capability acts on individual records this clause's data_scope narrows it too, but the name in this release is not per-record — 'member-lifecycle' is bounded by the app contexts the credential can reach, so data_scope does not narrow it.
     */
    granted_capabilities?: string[] | undefined;
    /** roleIds this clause may compose into a delegated AccessProfile (additive role composition, roleIds) — orthogonal to data_scope/allowed_actions: narrows WHICH named roles this clause's authority may hand out, not how much data it reaches. Absent/null means no roleId restriction (default-open) — matching data_scope's existing default-open-for-reads posture; a tenant opts INTO the restriction by adding this field. Format-validated the same way roleId is everywhere else (lowercase letter first, then a-z 0-9 -, 3-31 characters). An empty list is rejected — omit the field for 'no restriction' instead — and a duplicate entry is rejected rather than silently deduplicated. Adoption note: once you add this field to a clause, that clause can only compose the roles the list NAMES — the restriction applies to every write the clause authorizes, not just newly-authored ones — so plan a rollout that names every roleId the restricted clause still needs BEFORE adding the field, not after. One further condition: a composed role that itself grants role-composing authority (profiles, users, keys or triggers with c, u or d, or a wildcard '*') must ALSO carry an assignable_roles no broader than yours, or composing it would hand the delegate unrestricted composing power one hop later; an ordinary role that grants no composing authority of its own needs no change, and composing one keeps working exactly as it does today. */
    assignable_roles?: string[] | undefined;
}
