// This file was auto-generated by Fern from our API Definition.

import type * as Vectros from "../index.js";

/**
 * A declared trigger rule. A record write on the named schema runs the referenced script asynchronously under this rule's own grant.
 */
export interface TriggerRuleResponse {
    /** Unique identifier for this trigger rule (deterministically derived from tenant, context, and name — opaque, do not assume any particular format). */
    id?: string | undefined;
    /** The trigger's referenceable name. */
    name?: string | undefined;
    firingSource?: Vectros.FiringSourceRequest | undefined;
    scriptRef?: Vectros.ScriptRefRequest | undefined;
    /** Role composition granted to this trigger's principal, or null when the grant is inline (see 'scopes'). */
    roleIds?: string[] | undefined;
    /** Inline scope clauses granted to this trigger's principal, or null when the grant is role-composed (see 'roleIds'). */
    scopes?: Vectros.ScopeClause[] | undefined;
    /** The declared operation manifest, or null when no manifest was declared (the full grant applies). Enforced at execution: a verb outside the manifest is refused even when the grant would allow it. */
    manifest?: string[] | undefined;
    /** The record fields this rule projects into its script's `input.record` (and `input.previous` on UPDATE), in declared order. An empty list projects the record's identity only. Null only on a rule declared before this field existed, which projects nothing. */
    fields?: string[] | undefined;
    /** The TYPE_SERVICE principal id minted for this trigger, or null if not yet set. */
    principalId?: string | undefined;
    /** The name of the blueprint that provisions this trigger, or null for a trigger you manage yourself. `vectros bootstrap --blueprint` deletes only the triggers carrying its own blueprint's name, so a trigger belonging to another blueprint — or created directly through this API — is never removed by someone else's apply. */
    provisionedBy?: string | undefined;
    /** true if this request created a new trigger rule (HTTP 201); false if it returned or reconciled an existing one (HTTP 200). */
    created?: boolean | undefined;
    /** When this trigger rule was first created, as an ISO-8601 UTC timestamp. */
    createdAt?: string | undefined;
    /** When this trigger rule was last modified, as an ISO-8601 UTC timestamp. */
    updatedAt?: string | undefined;
}
