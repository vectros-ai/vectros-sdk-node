// This file was auto-generated by Fern from our API Definition.

import type * as Vectros from "../index.js";

/**
 * The outcome of a synchronous script execution. Returned only after the script's writes have committed — on any failure the body is the standard error envelope and nothing the script staged is committed.
 */
export interface ScriptExecuteResponse {
    /** Whatever your script returned — any JSON value (object, array, string, number, boolean or null), converted the way `JSON.stringify` would convert it. The platform does not inspect or validate it and makes no promise about its shape; treat it as your own contract between your script and your client. Capped at 256 KB serialised (16 KB when the request carried an `Idempotency-Key`); a larger value is refused with `RESULT_TOO_LARGE` before anything is written — return ids rather than payloads. */
    result?: (unknown | null) | undefined;
    execution: Vectros.Execution;
}
