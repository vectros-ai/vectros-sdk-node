// This file was auto-generated by Fern from our API Definition.

/**
 * Script execution time this period — trigger rules and synchronous `POST /v1/scripts/execute` calls together — and the surcharge it incurred. The millisecond figures are SUPPORTING detail so you can verify the charge — the billing unit is credits, never milliseconds. To check it yourself: billableMillis is totalMillis, plus 200 ms per execution, less includedMillis; the charge is then 1 credit per 100 seconds of billableMillis, with sub-credit remainders carried forward within the period rather than rounded away. Because remainders carry, compare creditsMilli against your period-to-date total rather than against a single execution.
 */
export interface ExecutionSection {
    /** Script executions metered this period, trigger and synchronous. Counts every execution that ran your script, including those that ended in an error and those that owed no charge; excludes firings refused before your script ran, which are never metered. */
    executions?: number | undefined;
    /** Total measured wall-clock time across those executions, in milliseconds */
    totalMillis?: number | undefined;
    /** Execution time included at no charge and actually applied against your execution time this period. A simple record write earns 350 ms; a document ingest 1000 ms; a read charged as overage 150 ms (a read inside your plan's allowance earns nothing). An indexed, oversized or foldered write earns a different amount because it is priced differently — this figure is the exact total applied, so you do not have to reconstruct it. Note it reports the allowance CONSUMED, not the allowance earned: an execution that finished well inside its included time contributes only what it used. It also absorbs the full duration of any execution we do not charge for at all — an internal platform error, for instance — so this figure can exceed what your own operations earned. Both are what keep the arithmetic above exact. */
    includedMillis?: number | undefined;
    /** The portion of that time actually charged, after the 200 ms per-execution baseline and the included time above */
    billableMillis?: number | undefined;
    /** Credits charged for execution time (rounded down; see creditsMilli for the exact value — a charge this small usually rounds to 0 whole credits) */
    credits?: number | undefined;
    /** credits in milli-credit precision (exact). 1 credit = 1000 milli-credits, so 1500 billable milliseconds is 15 milli-credits. */
    creditsMilli?: number | undefined;
}
