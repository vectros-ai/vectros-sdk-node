// This file was auto-generated by Fern from our API Definition.

/**
 * Narrow existence signal for a user lookup by email, scoped to a single app context.
 */
export interface UserExistsResponse {
    /** Whether a user with the given email holds an access profile in the given app context. */
    exists: boolean;
    /** The matching user's Vectros-assigned UUID. Present only when `exists` is true. */
    userId?: string | undefined;
    /** The matching user's lifecycle status. Present only when `exists` is true. */
    status?: UserExistsResponse.Status | undefined;
}

export namespace UserExistsResponse {
    /** The matching user's lifecycle status. Present only when `exists` is true. */
    export const Status = {
        Active: "ACTIVE",
        Suspended: "SUSPENDED",
        Pending: "PENDING",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
}
