// This file was auto-generated by Fern from our API Definition.

/**
 * What fires the trigger.
 */
export interface FiringSourceRequest {
    /** The platform-assigned schema id this rule watches. */
    schemaId: string;
    /** Which record lifecycle event fires this rule. The script reads it as `input.event` (`CREATE`, `UPDATE` or `DELETE`), alongside `input.schemaId`, `input.recordId` (the id `vectros.records.get` accepts), `input.userId` and `input.scope.<namespace>` (the record's stamped ownership), `input.record` (the rule's declared `fields`, from the written row — or the deleted row on DELETE) and, on UPDATE only, `input.previous` (the same fields from the row before the write). */
    event: string;
}
