// This file was auto-generated by Fern from our API Definition.

/**
 * Facts about the execution itself.
 */
export interface Execution {
    /** The execution's correlation id — the same value stamped on the request's log entry. Quote it to support. */
    id?: string | undefined;
    /** Wall-clock milliseconds the execution took, as metered for billing — the same span reported under `execution` on `GET /v1/usage`. */
    durationMs?: number | undefined;
}
